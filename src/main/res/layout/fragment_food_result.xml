<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add"
        android:id="@+id/button_add_result"
        android:enabled="true"
        android:layout_gravity="end"
        android:layout_below="@+id/spinner_serving_quantity"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/edit_to_add" />

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spinner_serving_quantity"
        android:spinnerMode="dropdown"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/edit_quantity"
        android:layout_alignBottom="@+id/edit_quantity" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="4"
        android:id="@+id/edit_to_add"
        android:text="25g"
        android:textAlignment="center"
        android:layout_below="@+id/spinner_serving_quantity"
        android:layout_alignParentEnd="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="4"
        android:id="@+id/edit_quantity"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:text="1.0"
        android:hint="quantity"
        android:textAlignment="center" />

</RelativeLayout>